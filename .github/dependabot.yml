version: 2
updates:
  # Actions are pinned to commit SHAs; Dependabot keeps those pins current and
  # rewrites the trailing version comment.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    commit-message:
      prefix: ci
